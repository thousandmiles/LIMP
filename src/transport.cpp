#include "limp/transport.hpp"

namespace limp
{

    // Transport is an interface - no implementation needed
    // Concrete implementations will be provided by users or future extensions

} // namespace limp
